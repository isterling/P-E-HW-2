background(255);
size(500,500);


//Balls, balls everywhere



for (int x = 20; x < 480; x = x+25) {
  for (int y = 20; y < 480; y = y+25) {
      fill(random(100),100,100);
      colorMode(HSB, 100);
      noStroke();
      ellipse (x, y, 20, 20);
  }
}

